version: '3'
services:
  python:
    restart: always
    build: ./docker/python
    working_dir: '/root/src'
    tty: true
    volumes:
      - .:/root/src
    ports:
      - 8001:8001
    environment:
      - PORT=8001